# SoftwareEngineeringI-Gui

Logger - log4j

log4j.xml - log4j configuration file, must be located at src/main/java/log4j2.xml

=====usage
logger.getLogger(); // name of class as parameter to getLogger is optional.

